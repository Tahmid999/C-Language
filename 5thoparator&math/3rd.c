#include<stdio.h>
#include<math.h>
int main() {

    float r,area;
    printf("enter cercle redius >> ");
    scanf("%f",&r);
    area = (M_PI*r*r);
    printf("area of cercle is >> %.3f",area);


    return 0;
}